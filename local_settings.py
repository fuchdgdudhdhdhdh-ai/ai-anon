"""
ЭТОТ ФАЙЛ — ЕДИНСТВЕННОЕ МЕСТО, КОТОРОЕ СТОИТ РУКАМИ ПРАВИТЬ В КОДЕ.
Все секреты (токены, ключи) задаются НЕ здесь, а в переменных окружения
Render / файле .env — см. README.md.
"""

# Имя персонажа по умолчанию при первом запуске
# (после первого запуска можно будет сменить прямо в боте — TODO: команда /rename)
DEFAULT_PERSONA_NAME = "Алексей Нейросеть"

# Шаблон системного промпта для нейросети.
# {persona_name} и {examples} подставляются автоматически, не убирай их.
SYSTEM_PROMPT_TEMPLATE = (
    "Ты — ИИ-персонаж по имени {persona_name}. Все собеседники знают, что ты "
    "нейросеть, имитирующая стиль общения владельца аккаунта — это не секрет, "
    "не пытайся скрывать этот факт, если спросят напрямую. "
    "Общайся живо и коротко, в той же манере, лексике и длине фраз, что показано "
    "в примерах ниже. Не выдумывай факты о реальной жизни владельца (планы, "
    "события, отношения), если не уверен — отвечай уклончиво и по-дружески, "
    "как обычно отвечает живой человек, когда не готов сходу сказать точно."
)

# --- Параметры генерации ответа нейросетью (Groq) ---
GROQ_TEMPERATURE = 0.8          # 0 = максимально предсказуемо, 1+ = более творчески
GROQ_MAX_TOKENS = 400           # максимальная длина одного ответа

# Сколько последних реплик диалога помнить в рамках одного разговора
HISTORY_MESSAGES = 10

# --- Сбор примеров стиля из истории чата ---
STYLE_SAMPLES_LIMIT = 300       # сколько сообщений владельца просматривать
STYLE_SAMPLES_IN_PROMPT = 40    # сколько из них реально подставлять в промпт

# Максимальная длина фразы-триггера и ответа/ссылки (защита от случайного мусора)
TRIGGER_PHRASE_MAX_LEN = 100
TRIGGER_CONTENT_MAX_LEN = 1000
